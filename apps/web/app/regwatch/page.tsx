import styles from "../stub.module.css";

export default function RegWatchPage() {
  return (
    <section className={styles.stub}>
      <p className={styles.eyebrow}>Planned</p>
      <h1>RegWatch</h1>
      <p>
        Source health, events, approval and replay comparison land in a later
        sprint. Navigation stub only — no fabricated regulatory events.
      </p>
    </section>
  );
}
