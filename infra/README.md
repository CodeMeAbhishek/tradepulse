# TradePulse infra

## Active: GCP (demo)

See **[infra/gcp/README.md](gcp/README.md)** for live Cloud Run URLs and redeploy scripts.

Project: `tradepulse-demo` · Region: `asia-south1`

## Legacy: AWS (retired)

Previous ECS/ALB stacks in `ap-south-1` are down (`NXDOMAIN` on old ELB hostnames). Scripts remain under `infra/deploy-api.ps1`, `infra/deploy-web.ps1`, `infra/api-ecs.yaml`, `infra/web-ecs.yaml` for reference only.
